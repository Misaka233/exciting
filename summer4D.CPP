/*
一   首先答案可以是正数。

      那就简单了，先把所有的数进行升序排列，因为ga数组最小的数一定比gb数组最大的数大。所以排列后一定会在相连的位置得到答案。所以就从左到右便利分割点。

左边作为ga数组，右边作为gb数组，然后进行原数组的模拟，然后判断下cost有没有超过c即可。

如果发现不可能为正数，那就必须是负数了。

二  负数的情况

    因为每交换一对数会花费2，而且最后还可以进行一次交换（0的时候）.所以就简单了，只需要判断一对一对交换后，最后要交换的那个位置，2种情况取（就是上面最小的下来，下面最大的上去）最大的结果就可以了。奇数偶数是一样的做法。



然后，第一步取正数的过程中，因为a数组是有大小的所以可以直接二分位置。因为题目数据量不是很大，为求方便我就直接遍历了。
*/
#include<iostream>
#include<cstdio>
#include<cstring>
#include<string>
#include<algorithm>
using namespace std;
