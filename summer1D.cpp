#include <iostream>
#include <cstdio>
#include <cmath>
#include <cstring>
#include <algorithm>
using namespace std;
const int maxn=80;
string a[maxn][maxn];
int b[maxn][maxn];
int main()
{
    int t;
    cin>>t;
    for(int cas=1;cas<=t;cas++)
    {
        int n,m,l,u;
        cin>>n>>m>>l>>u;
        for(int i=0;i<n;i++)
            for(int j=0;j<m;j++)
            {
                cin>>a[n][m];
            }
    }
    return 0;
}
